import type { Metadata } from "next";

export const metadata: Metadata = { title: "Boldin — Withdrawal Order — Cez Corpus" };

export default function BoldinWithdrawalOrder() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <h1 className="font-(family-name:--font-display) text-display-lg font-medium">
        Boldin — Withdrawal Order
      </h1>
    </div>
  );
}
